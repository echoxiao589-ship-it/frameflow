# 本地访问地址，默认仅监听本机。
HOST=127.0.0.1
PORT=4173
# demo: 本地脚本模板、SVG 示例、真实 HTML 动画、Windows 中文语音。
# gateway: 使用自己的生成服务，协议见 README.md。
AI_PROVIDER=demo
AI_GATEWAY_URL=
AI_GATEWAY_KEY=
# 可选：保存项目和媒体的绝对路径；默认 ./data
# DATA_DIR=
